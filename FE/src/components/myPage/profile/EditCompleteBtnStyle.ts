import styled from 'styled-components/native';
import {primary} from '../../../constants/colors';

const Wrapper = styled.Pressable``;

const BtnText = styled.Text`
  color: ${primary};
  font-size: 20px;
`;

export {BtnText, Wrapper};

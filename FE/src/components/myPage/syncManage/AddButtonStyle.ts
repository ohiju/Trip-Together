import styled from 'styled-components/native';
import {primary} from '../../../constants/colors';

const Wrapper = styled.Pressable``;

const AddText = styled.Text`
  color: ${primary};
  font-size: 18px;
`;

export {AddText, Wrapper};

import styled from 'styled-components/native';

const Wrapper = styled.View``;

const NoResultView = styled.View`
  padding: 15px;
  align-items: center;
`;

const NoResultText = styled.Text`
  font-size: 16px;
`;

export {NoResultText, NoResultView, Wrapper};

import styled from 'styled-components/native';

const Wrapper = styled.View`
  flex: 1;
  align-items: center;
  justify-content: center;
`;

const PlaceholderView = styled.View``;

const Placeholder = styled.Text`
  font-size: 14px;
`;

export {Placeholder, PlaceholderView, Wrapper};

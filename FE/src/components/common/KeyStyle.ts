import styled from 'styled-components/native';

const Wrapper = styled.Pressable`
  flex: 1;
  align-items: center;
  justify-content: center;
  margin-top: 20px;
`;

const KeyText = styled.Text`
  font-size: 25px;
`;

export {KeyText, Wrapper};

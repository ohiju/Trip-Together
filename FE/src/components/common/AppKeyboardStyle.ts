import styled from 'styled-components/native';

const Wrapper = styled.View`
  height: 60%;
  margin-bottom: 20px;
`;

const Row = styled.View`
  flex: 1;
  flex-direction: row;
`;

export {Row, Wrapper};

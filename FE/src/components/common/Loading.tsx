import React from 'react';
import {LoadingText, Wrapper} from './LoadingStyle';

const Loading = () => {
  return (
    <Wrapper>
      <LoadingText>Loading...</LoadingText>
    </Wrapper>
  );
};

export default Loading;

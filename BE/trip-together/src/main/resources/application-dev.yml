spring:
  data:
    redis:
      host: j10a309.p.ssafy.io
      port: 6379
    mongodb:
      uri: mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@j10a309.p.ssafy.io:${DEV_TRIP_MONGODB_BINDING_PORT}/?authSource=admin
      database: ${MONGO_INITDB_DATABASE}
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create
  mvc:
    servlet:
      path: /api

EXCHANGE_RATE_API_KEY: ${EXCHANGE_RATE_API_KEY}
jwt:
  secret: ${JWT_SECRET_KEY}
